# ── Reference frontend (this repo root) ─────────────────────────────

# Mock mode (default): Next.js Route Handlers under /api
NEXT_PUBLIC_API_URL=/api

# Integration mode: point at YOUR backend (see backend/README.md)
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
